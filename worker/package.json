{
  "name": "kushiro-chat-auth-worker",
  "version": "1.0.0",
  "description": "釧路コミュニティチャット メール認証 Cloudflare Worker",
  "private": true,
  "scripts": {
    "setup":  "node setup.js",
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "logs":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
